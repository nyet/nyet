#!/usr/bin/perl
use strict;

my $running = 1;
while ($running) {
   foreach my $platform (@platforms) {
      #open SQLite database
      # get the next dev-*.txt file for $platform from SQLite.
      # close SQLite database
      # if there is no more dev-*.txt file for that platform
         $running = 0;
      
}
